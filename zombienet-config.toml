[relaychain]
default_command = "./target/release/predictor-node"
default_args = ["--validator", "-lruntime=debug"]

chain = "local"

  [[relaychain.nodes]]
  name = "alice"
  validator = true

  [[relaychain.nodes]]
  name = "bob"
  validator = true
